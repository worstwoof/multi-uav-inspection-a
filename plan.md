# 方案

本文件记录第一届天津市五校数学建模联赛 A 题的完整工作流。本轮只完成项目扫描、资料盘点、工程骨架和后续阶段依赖规划；不调用下游建模/编程/制图/写作/验收 skill，不编写求解代码，不运行现有求解程序，不生成最终数据图，不撰写论文正文，也不编译最终 PDF。

## 用户偏好与已确认约束

- 排版引擎：LaTeX。依据是 `task-spec.md` 明确要求分析当前参考 LaTeX 工程，且参考工程以 XeLaTeX 编译；后续沿用该工程的排版基线。
- 竞赛类型：第一届天津市五校数学建模联赛，A 题。
- 论文语言：中文。
- 子问题数量：3 个，题面已明确。
- 参考工程优先级：当前目录中的参考 LaTeX 工程高于内置比赛模板；只继承版式和宏包组织，不复制 B 题正文、公式、数据或结论。

## 本轮项目盘点

### 根目录与约束文件

- `task-spec.md`：已用 UTF-8 完整读取；内容保持原样，未修改、删减或覆盖。它要求严格按“读题 -> 读附件 -> 读参考格式 -> 分析 -> 建模 -> 代码 -> 运行 -> 验证 -> 图表 -> 论文 -> 编译 -> 检查”的顺序推进，并要求优先使用当前参考 LaTeX 工程。
- `A 题/`：题目 PDF、题目附件和已有结果文件。
- `参考格式/`：参考工程 ZIP，原始压缩包保留不动。

### 题目

- `A 题/TJMML_A.pdf`：3 页 A4 PDF，题名为“低空经济背景下的多无人机协同巡检路径优化问题”。
- 题面关键参数：I/II/III 级分别巡检 3/2/1 次；无人机巡航速度 55 km/h；单点巡检 5 min；坐标 1 单位对应 100 m；基地为 `(0,0)`；所有无人机 8:00 同时起飞并完成自身任务后回基地；问题一约束为 9 h；问题三加入按时间启用的圆形禁飞区。
- 三问主线：问题一先求 9 h 约束下的最低可行无人机数并最小化总体完成时间；问题二在问题一调度上保持总体效率并压缩工作时间极差；问题三加入动态禁飞区后重新规划，同时满足巡检、时间窗和负载均衡要求。

### 原始数据附件

- `A 题/附件1.xlsx`：4 个 sheet（Case1-Case4），字段为 `Point_ID`、`X_Coordinate`、`Y_Coordinate`、`Inspection_Level`。
  - Case1：52 点，I/II/III = 5/10/37，坐标范围 X=`25..1740`、Y=`5..1175`，总巡检次数 72。
  - Case2：99 点，I/II/III = 10/20/69，坐标范围 X=`2..88`、Y=`4..217`，总巡检次数 139。
  - Case3：100 点，I/II/III = 10/20/70，坐标范围约 X=`3.24542..977.601`、Y=`1.16433..981.97`，总巡检次数 140。
  - Case4：130 点，I/II/III = 13/26/91，坐标范围约 X=`6.925224..698.785045`、Y=`0.470231..691.02623`，总巡检次数 182。
- `A 题/附件2.xlsx`：4 个 sheet，字段为 `Zone_ID`、`Center_X`、`Center_Y`、`Radius`、`Start_Time`、`End_Time`。
  - Case1/2/3/4 分别有 3/4/6/8 个禁飞区；时间窗覆盖 08:30-17:00，Case4 的 Z8 为 `17:00-17:00` 的零时长边界数据，后续需明确其处理规则。
  - 禁飞区圆心和半径均使用附件坐标单位，后续需与线段-圆相交判断及飞行时间传播统一单位。

### 已有结果文件（只作为待复核基线）

- `A 题/result1.xlsx`：问题一调度结果，4/2/5/4 架无人机；Summary 中四个 Case 均标记任务完成且 9 h 内。汇总 `Tmax` 约为 8.4325/7.4817/8.3097/8.5809 h。
- `A 题/result2.xlsx`：问题二负载均衡结果，仍为 4/2/5/4 架；四个 Case 均标记任务完成且 9 h 内，`Delta` 约为 0.0151/0.000225/0.0132/0.0081 h。
- `A 题/result3.xlsx`：问题三动态禁飞区结果；Summary 的 `No-fly Valid` 四个 Case 均为 True，但 Case2/3/4 的 `Within 9 h` 为 False，`Tmax` 约为 11.5513/10.2260/10.4975 h，因此不能直接当作题目约束下的最终可行结果。
- 初始一致性检查曾按 Excel 原始序列条目统计为 72/139/140/182，但按用户补充的有效到达规则（A-A-A 只算 1 次、A-B-A 算 2 次）重新合并连续段后，三份结果各 Case 仅有 52/99/100/130 次有效到达，分别少 20/40/40/52 次；因此既有任务完成标记不能作为有效到达约束下的通过结论。当前未发现与这些结果对应的源代码或求解脚本，阶段3必须重新求解。

### 参考 LaTeX 工程

`参考格式/B题_嵌入式社区养老服务站的建设与优化问题.zip` 内含：`main.tex`、`sections/00_abstract.tex` 至 `06_evaluation_conclusion.tex`、`figures/` PNG 图集、`README_Overleaf编译说明.md`、`main_compiled_reference.pdf` 和 `compiled_test_main.pdf`。没有 `.cls`、`.sty` 或 `.bib` 文件，格式集中写在 `main.tex` 和章节文件中。

- 文档类：`ctexart[UTF8,a4paper,12pt]`；页边距 `2.72cm`；正文用 XeLaTeX；使用 `amsmath/amssymb`、`booktabs`、`graphicx`、`float`、`longtable`、`array`、`tabularx`、`hyperref`、`fancyhdr`、`lastpage`、`setspace`、`tikz`、`multirow`、`subcaption`、`makecell` 等宏包。
- 版式：1.40 倍行距，首行缩进 2em，段间距 0.28em，表格行距 1.18；中文 section 编号、阿拉伯小节编号；标题居中/加粗；页眉为队号 `005445B` 和 `Page <页码> of <总页数>`。
- 组织：`main.tex` 负责宏包、页眉、标题、摘要、目录和 `\input{sections/...}`；正文按章节拆分；表格直接写在 `.tex` 中；图片从 `figures/` 相对路径引入；没有独立参考文献数据库或代码环境约定。
- 编译说明指定 Overleaf/XeLaTeX，参考 PDF 为 23 页 A4，已成功编译。后续只复制其版式结构与编译方式，论文内容和图表全部基于 A 题真实结果重建。

## 项目工程骨架

本轮已创建以下空目录，后续阶段按约定填充：

```text
.
├── plan.md
├── todo.md
├── reports/
│   ├── ANALYSIS_MODELING_REPORT.md   # 阶段 2
│   ├── RESULTS_REPORT.md             # 阶段 3
│   ├── DRAWIO_REPORT.md              # 阶段 4
│   └── VERIFY_REPORT.md              # 阶段 6
├── code/                             # 阶段 3 的可复现代码
├── results/                          # 阶段 3 的 JSON/CSV/汇总结果
├── figures/                          # 阶段 3 数据图 + 阶段 4 非数据图
└── paper/
    └── sections/                     # 阶段 5 的 LaTeX 工程
```

原始 `A 题/`、`参考格式/` 和 `task-spec.md` 保持原路径与内容，不把参考 B 题文件复制到论文目录。

## 后续阶段输入、输出和依赖

### 阶段 2：赛题分析与建模设计 - `2analysis-modeling`

- 输入：`task-spec.md`、`A 题/TJMML_A.pdf`、`A 题/附件1.xlsx`、`A 题/附件2.xlsx`、已有 `result1/2/3.xlsx`（用于识别基线及矛盾）、本文件的题目盘点。
- 工作：统一单位和时间基准；展开巡检任务；给出无人机数量理论下界与最低可行性定义；建立三问连续的任务分配、路径、负载均衡和动态禁飞区时空约束模型；说明歧义（尤其多次巡检是否允许非连续、禁飞区边界和零时长窗口）。
- 输出：`reports/ANALYSIS_MODELING_REPORT.md`，包含变量、参数、目标函数、约束、算法接口、验证清单和供阶段3调用的数据结构。
- 依赖：阶段 3 必须以此报告为唯一建模接口，不在代码阶段擅自改变题意解释。

### 阶段 3：编程实现和图表生成 - `3coding-visual`

- 输入：阶段 2 报告、附件1/2 原始数据、阶段 1 发现的已有结果文件。
- 工作：实现数据读取、任务展开、距离/飞行时间、任务分配与路径优化、负载均衡、动态禁飞区几何与时间传播、可行性检查；固定随机种子并实际运行全部四个 Case。
- 输出：`code/`（可复现源码）、`results/`（JSON/CSV 单一真源和汇总）、`reports/RESULTS_REPORT.md`、`figures/` 中由真实结果生成的数据图（路线、负载、比较、禁飞区影响等）。
- 依赖：阶段 4 只能读取阶段 3 的真实结果来画数据图；阶段 5 只能引用阶段 3 的结构化数值，不能手填或编造结论。

### 阶段 4：流程与架构图绘制 - `4drawio`

- 输入：阶段 2 模型报告、阶段 3 结果报告和已生成的 `figures/` 数据图。
- 工作：仅绘制技术路线、任务分配/路径算法流程、动态禁飞区处理逻辑等非数据型图；不重复绘制真实路线和统计图。
- 输出：`figures/*.drawio`、对应 PDF，以及 `reports/DRAWIO_REPORT.md`。
- 依赖：需要阶段 2 的模型术语和阶段 3 的实际处理流程；阶段 5 将这些图作为解释性图插入论文。

### 阶段 5：竞赛论文撰写 - `5writing`

- 输入：阶段 2/3/4 报告、`results/` 结构化结果、`figures/` 全部图、参考工程 `main.tex`/`sections` 的版式规则。
- 工作：建立 A 题中文 LaTeX 论文，沿用 ctexart、页眉页脚、章节层级、三线表和 XeLaTeX 编译方式；结果表和图表必须来自阶段 3/4，正文不重复叙述表格、不编造数值。
- 输出：`paper/main.tex`、`paper/sections/*.tex`、必要的参考文献和论文辅助文件；不覆盖 `参考格式/` 原始 ZIP。
- 依赖：阶段 6 只有在论文源文件、结果真源和图表齐备后才能验收。

### 阶段 6：验证和验收 - `6verity`

- 输入：阶段 2-5 全部产物、原始题目/附件、`task-spec.md`。
- 工作：核对三问、四个 Case、任务次数、路线回基地、9 h 约束、动态禁飞时间重叠、结构化结果与论文数值一致性；运行 XeLaTeX 并逐页检查 PDF；扫描 TODO/PLACEHOLDER/缺图/未解析引用。
- 输出：`reports/VERIFY_REPORT.md`、验收后的 `paper/main.pdf` 及提交就绪说明。
- 依赖：阶段 6 是终检，不应反向修改已有结果而不重新运行验证。

## 依赖关系与风险控制

```text
原始题目/附件 + 已有结果 + 参考 LaTeX
                 │
                 ▼
        阶段2 建模报告
                 │
                 ▼
        阶段3 代码/结果/数据图
                 ├──────────────┐
                 ▼              ▼
        阶段4 非数据图      阶段5 LaTeX 论文
                 └──────┬───────┘
                        ▼
                 阶段6 验收/PDF
```

- 主要风险一：已有 `result3.xlsx` 的 Case2-4 超过 9 h；后续必须明确问题三是否允许增加无人机数量或只在问题一的 `Nmin` 上重排，并以题面约束为准。
- 主要风险二：巡检次数按有效到达计数，连续相同 Point_ID 只能算一次；阶段3必须禁止相邻同点、按到达段验证 3/2/1 次，并重新淘汰已有 Excel 中的连续重复路线。
- 主要风险三：问题三同时需要线段与圆的几何相交和飞行时间窗重叠判断，不能用固定比例增加距离替代；Case4 Z8 的零时长窗口需要单独记录处理假设。
- 主要风险四：已有 Excel 没有对应源代码，且距离/时间是否包含基地往返、等待或绕行需从后续程序和验证日志重新确认；现有结果只能作为基线，不作为论文最终真源。
- 阶段边界：本轮完成后立即停止；后续阶段必须按上述顺序显式调用对应 skill。
